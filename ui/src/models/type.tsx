export type Category = {
    categoryId: string,
    categoryName: string,
    numOfMovies: number,
    imageUrl: string
}

export type Movie = {
    movieId: string,
    title: string,
    averageRatingOfMovie: number,
    imageUrl: string,
    description: string
}